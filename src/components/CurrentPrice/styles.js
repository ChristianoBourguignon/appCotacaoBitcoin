import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
    },
    currentPrice: {
        color: '#ff0000',
        fontSize: 32,
        fontWeight: 'bold',
        paddingTop: 20,

    },
    textPrice: {
        fontWeight: 'bold',
        fontSize: 16,
        color: '#fff'

    },
})
export default styles;